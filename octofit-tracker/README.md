# OctoFit Tracker (placeholder)

This is a tiny placeholder file added so the `build-octofit-app` branch contains a commit and a pull request can be created.

Planned work will include:
- Backend (Django + REST)
- Frontend (React)
- Authentication and activity tracking
